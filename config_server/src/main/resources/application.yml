spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jhq9/config_center/
          search-paths: config_rep
          username: jhq9
          password: whyjhqlove512
server:
  port: 8888
